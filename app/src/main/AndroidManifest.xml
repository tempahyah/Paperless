<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.enovatesoft.paperless">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:name=".app.AppController"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".LoginActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".InformationRequirements"
            android:label="Information Requirements" />
        <activity
            android:name=".CameraUI"
            android:label="Document Capture" />
        <activity
            android:name="com.theartofdev.edmodo.cropper.CropImageActivity"
            android:theme="@style/Base.Theme.AppCompat" />
        <activity
            android:name=".NewCustomer"
            android:label="@string/app_name"
            android:launchMode="singleTop" />
        <activity
            android:name=".RegisterActivity"
            android:label="User Sign Up"
            android:launchMode="singleTop"
            android:windowSoftInputMode="adjustPan" />
        <activity
            android:name=".About"
            android:label="About Paperless" />
        <activity android:name=".ScanActivity" />
        <activity android:name=".CustomerDetails" />
        <activity android:name=".QRCapture" />

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />

        <activity
            android:name=".TransactionPage"
            android:label="Transactions" />
        <activity
            android:name=".DocumentsRequired"
            android:label="Capture Required Documents" />
        <activity
            android:name=".CameraCapture"
            android:label="Scan Document" />
        <activity android:name=".SearchActivity" />
        <activity android:name=".TransactionSearch"></activity>
    </application>

</manifest>